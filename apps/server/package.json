{
    "name": "@silent-pix/server",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "dev": "tsx watch src/index.ts",
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@hono/node-server": "latest",
        "@silent-pix/shared": "workspace:*",
        "dotenv": "latest",
        "hono": "latest"
    },
    "devDependencies": {
        "tsx": "latest"
    }
}
